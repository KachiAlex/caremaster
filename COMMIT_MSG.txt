Implement emergency and code-red notification routing

Emergency button (SOS):
- Added emergency_alerts case to the server-side notification dispatcher.
  Previously, creating an emergency alert produced zero notifications.
- Now notifies ALL doctors, nurses, and admins in the institution with
  CRITICAL priority when the SOS button is pressed.
- Notification includes: who triggered it, emergency type, severity,
  description, location, and contact number so responders have full
  context to reach the person in need.
- Also notifies the client's assigned doctor specifically.
- Triggers web push notifications for offline/PWA delivery.

Observation code red (nurse reports):
- Improved the nurse_reports code red routing.
- Previously, code red notified ALL doctors in the institution.
- Now looks up the client's assigned doctor from the clients table
  and notifies that specific doctor + all admins.
- Falls back to all doctors if no assigned doctor is found.
- Code red notifications now use TYPE.EMERGENCY (instead of SYSTEM)
  for higher visibility and distinct handling in the UI.

Frontend EmergencyButton:
- Packed emergencyType, triggeredByName, contactNumber, userRole, and
  clientName into the metadata jsonb field so they survive the backend's
  writable-field allowlist and are available for notification dispatch.

Bumped SW cache to v39. Backend restarted via PM2.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
